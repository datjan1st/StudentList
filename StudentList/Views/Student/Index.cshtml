@model IEnumerable<Student>
@{
    ViewData["Title"] = "Index";
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Student List</h2>
        </div>
    </div>
    <div class="col-6">
        <a asp-controller="Student" asp-action="Create" class="btn btn-primary">
            Create New Students
        </a>
    </div>
    <br /><br />
<table class = "table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Student Name
            </th>
            <th>
                Course Name
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="50%">
                    @obj.StudentName
                </td>
                <td width="30%">
                    @obj.CourseName
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Student" asp-action="Edit" asp-route-studentid="@obj.StudentId"
                        class = "btn btn-primary mx-2">Edit</a>
                        <a asp-controller="Student" asp-action="Delete" asp-route-studentid="@obj.StudentId"
                        class = "btn btn-primary mx-2">Delete</a>
                    </div>
                </td>
                <th></th>
            </tr>
        }
    </tbody>
</table>
</div>

